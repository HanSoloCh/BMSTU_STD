#!/usr/bin/env bash
set -euo pipefail

echo "[hook] Auto-formatting code..."
./gradlew addTrailingNewlines ktlintFormat --quiet

echo "[hook] Running detekt (with auto-correct), ktlint, halstead..."
./gradlew detekt ktlintCheck halstead --quiet
echo "[hook] Static analysis passed."


